<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css"
    />

    <style>
      body {
        font-family: sans-serif;
        margin: 0;
        padding: 0;
      }

      .container-fluid {
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        background-size: cover;
        background-repeat: no-repeat;
        background-position: center;
        background-image: url("https://source.unsplash.com/random/1920x1080/?login,theme");
      }

      .card {
        background-color: rgba(255, 255, 255, 0.8);
        border-radius: 10px;
        padding: 30px;
      }

      h1 {
        font-weight: bold;
        color: #ff69b4; /* Pink color for heading */
      }

      label {
        font-weight: bold;
        color: #e83e8c; /* Pink color for labels */
      }

      input[type="email"],
      input[type="password"] {
        border: 1px solid #ccc;
        padding: 10px;
        margin-bottom: 10px;
        border-radius: 5px;
      }

      button[type="submit"] {
        background-color: #fff;
        color: #007bff;
        border: none;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
      }

      .login-link {
        margin-top: 15px;
      }
    </style>
  </head>
  <body>
    <div class="container-fluid">
      <div class="row w-100">
        <div class="col-md-6 mx-auto">
          <div class="card card-body shadow-sm">
            <h1 class="text-center mb-4">Login</h1>
            <form action="/login" method="post">
              <div class="mb-3">
                <label for="email" class="form-label">Email:</label>
                <input
                  type="email"
                  name="email"
                  class="form-control"
                  required
                />
              </div>
              <div class="mb-3">
                <label for="password" class="form-label">Password:</label>
                <input
                  type="password"
                  name="password"
                  class="form-control"
                  required
                />
              </div>
              <button type="submit" class="btn btn-primary btn-block">
                Login
              </button>
              <div class="login-link">
                <p>Create New Account? <a href="/signup">Signup</a></p>
              </div>
            </form>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
